
#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an org.hyprland.COMMAND app id for styling.

exec setsid uwsm-app -- ghostty --class=org.hyprland.$(basename $1) -e "$1" "${@:2}"
